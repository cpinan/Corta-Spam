# Corta Spam — Session State (2026-07-30)

## App Identity
- Name: Corta Spam
- Package: org.carlospinan.bloqueador.app
- Platform: Android (KMP), iOS deferred
- License: MIT
- GitHub: https://github.com/cpinan/Corta-Spam (empty, needs initial push)

## Changes (2026-07-30)

### Call Log improvements
- Call back action: tapping a number → "Call back" launches `ACTION_DIAL`
- Local timestamps: each entry shows formatted local time ("Jul 30, 2026 · 14:30"), "Now" for <60s
- `onCallBack` wired through CallLogScreen → AppNavHost → App → MainActivity

### Navigation
- All `navController.navigate()` calls use `launchSingleTop = true` — fixes screen reload on double-tap

### Incoming Call UI
- InCallActivity: KeyguardManager.dismissKeyguard → full-screen takeover on lock screen
- Manifest: android:showWhenLocked="true" + android:turnScreenOn="true"

### UI
- Spam provider toggle hidden from SettingsScreen (backend preserved)
- Auto-responder marked Experimental in Settings + AutoResponderScreen

### i18n
- 4 locales: EN (values/), ES (values-es/), PT-BR (values-pt/), HI (values-hi/)
- New string keys: call_log_action_callback, call_log_action_label, call_log_rule_label,
  call_log_time_label, call_log_just_now, autoresponder_experimental_badge,
  stats_loading, stats_blocked_count

### De-hardcoding
- Removed hardcoded UI strings: "Loading...", "X blocked", "Privacy Policy",
  "Terms & Conditions", "View", "OK", "Action", "Rule" → all now use stringResource

## Completed

### Core (M0-M10)
- Rules engine: manual block, allowlist, patterns, countries, quiet hours, action rules (backend), spam provider stub
- Call screening via PassthroughInCallService
- Auto-responder (TTS + custom audio)
- Call log with rule detail
- Stats (day/week/month)
- Backup/restore (JSON export/import)
- Bundled spam heuristics
- Welcome + onboarding flow

### Adaptive Layout (M12)
- 3-tier window size: Compact (<600dp), Medium (600–839dp), Expanded (≥840dp)
- AdaptiveScaffold: bottom bar (Compact), nav rail (Medium/Expanded)
- AdaptiveContent: caps at 600dp, centers on wider screens
- Home: two-column on Medium/Expanded (stats left, quick grid right)
- CallLog: list-detail two-pane on Expanded (340px list + detail pane)
- BlockListHub: FlowRow 2-col Compact, 4-col wider
- Edge-to-edge: WindowInsets on AdaptiveScaffold content boxes
- configChanges on MainActivity + InCallActivity

### Iconography
- 21 custom icons (24×24, 1.8dp stroke, round caps)
- Brand: navy #17233C + coral #EF6A5B + cream #FFF4E6
- Android launcher foreground + monochrome + adaptive icon XMLs

### Bug Fixes
- Manual block precedence: MANUAL_BLOCK > ALLOWLIST
- Edge-to-edge: WindowInsets.safeDrawing on scaffold content
- Scroll fix: AdaptiveContent contentModifier
- Rotation: configChanges on both activities
- Clock: Material3 TimePicker replaced text fields
- Navigation: launchSingleTop prevents double-tap reload

### Tests (28+ files, 158+ cases)
- Rules engine, ViewModels, settings, adaptive, UI screens, onboarding, backup round-trip

### Tooling
- install_android.sh
- render_ui_icons.mjs, regenerate-ios-icons.sh
- ktlint + CI (.github/workflows/ci.yml)

## Remaining
- Google Play submission
- iOS CallDirectoryProvider extension
- Export file encryption
- Spam provider live integration
- Settings list-detail two-pane (Expanded)
- Home recent blocked calls list (dashboard)

## Build Commands
```sh
./gradlew :shared:testDebugUnitTest          # 158+ tests
./gradlew :androidApp:assembleDebug           # Build APK
./install_android.sh                          # Install + launch on device
node design/iconography/render_ui_icons.mjs   # Regenerate icons
```
